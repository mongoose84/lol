// Could not come up with a good descriptive name. Rename when you get a better name

public static class CuntyMcCuntface
{
    // Filled in at runtime (see line above)
    public static IServiceProvider ServiceProvider { get; set; } = null!;
}